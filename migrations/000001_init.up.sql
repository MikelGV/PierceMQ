-- 000001_init: schema scaffolding.
-- Placeholder: add CREATE TABLE statements here in future migrations.
-- This file intentionally changes nothing; it proves the migrate
-- pipeline (embed -> db-migrate one-shot -> primary via PgBouncer
-- write pool) works end to end and anchors version 1.
SELECT 1;
