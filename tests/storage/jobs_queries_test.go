package storage_test

import "testing"

func CreateJob_test(t *testing.T) {
	t.Run("job gets created successfully", func(t *testing.T) {})

	t.Skip("job fails to be created due to bad payload")

	t.Skip("job creation failure due to network issues")
}
